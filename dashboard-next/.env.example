# Browser-safe external FastAPI origin. Set separately in Vercel Preview and Production.
NEXT_PUBLIC_API_BASE_URL=
# Canonical public frontend URL used for metadata.
NEXT_PUBLIC_SITE_URL=
# Comma-separated frontend origins belong in the FastAPI environment, not here:
# CORS_ORIGINS=https://preview.example.com,https://example.com
